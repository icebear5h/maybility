-- Create database tables
-- This script will be automatically executed by Prisma migrations

-- Enable UUID extension if using PostgreSQL
-- CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- The actual table creation is handled by Prisma migrations
-- Run: npx prisma migrate dev --name init

-- Sample data can be inserted here for development
-- INSERT INTO users (id, name, email, created_at, updated_at) 
-- VALUES ('sample-user-id', 'Test User', 'test@example.com', NOW(), NOW());
